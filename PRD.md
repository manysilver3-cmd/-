# 📄 제품 요구사항 정의서 (PRD: Product Requirements Document)

## 1. 프로젝트 개요
* **서비스명**: 칼로리 & 운동 시간 계산기 (Calorie and Workout Calculator)
* **목적**: 사용자가 자연어(텍스트)로 입력한 섭취 식단 목록을 바탕으로 개별 및 총 섭취 칼로리를 산출하고, 이를 소모하는 데 필요한 대표적인 유산소 운동(걷기, 러닝, 자전거)별 소모 시간을 직관적으로 제공하여 건강한 식습관 및 활동 관리를 지원합니다.
* **타겟 사용자**: 식단 칼로리와 운동 필요량을 빠르고 직관적으로 확인하고자 하는 현대인 및 다이어터

---

## 2. 핵심 성공 지표 및 목표 (Success Metrics)
1. **응답 속도 (Performance)**: 식단 입력 후 계산 버튼 클릭 시 **3초 이내** 결과 렌더링 완료 (실측 500ms 이내)
2. **인식 정확도 (Accuracy)**: 한국어 다빈도 음식명, 수량어(한/두/세/반 등), 단위(개/조각/공기/캔 등) 파싱 정확도 90% 이상
3. **결과 순서 보장 (Output Sequence)**:
   - 1라인/섹션: 개별 음식별 칼로리 (수량 × 단위 칼로리)
   - 2라인/섹션: 총칼로리량 하이라이트
   - 3라인/섹션: 추천 운동 종목 목록 (걷기, 러닝, 자전거)
   - 4라인/섹션: 추천 운동별 소모 시간 (분/시간 단위)
4. **사용자 경험 (UX & Resilience)**:
   - 빈 입력값, 음수 수량 등 유효하지 않은 입력에 대한 즉각적인 피드백
   - 로딩 스피너 및 스켈레톤 UI를 통한 체감 대기시간 최소화
   - 미인식 음식 또는 오류 발생 시 복구 경로(다시 시도 버튼) 제공

---

## 3. 사용자 스토리 (User Stories)
* **US-01**: 사용자는 여러 줄 또는 쉼표로 구분하여 오늘 먹은 음식 목록을 자유롭게 텍스트로 입력할 수 있다.
* **US-02**: 사용자는 계산 버튼을 누르거나 `Ctrl+Enter` 단축키를 눌러 즉시 결과를 조회할 수 있다.
* **US-03**: 사용자는 개별 음식 칼로리, 총합 칼로리, 추천 운동 종목 및 필요 운동 시간을 한눈에 비교할 수 있다.
* **US-04**: 사용자는 이전 계산 기록을 확인하고 원클릭으로 다시 계산할 수 있다.
* **US-05**: 사용자는 잘못된 입력이나 오류 발생 시 친절한 안내 메시지와 함께 즉시 재시도할 수 있다.

---

## 4. 상세 기능 명세 (Functional Specifications)

### 4.1 텍스트 입력 및 유효성 검사
* **Multi-line Text Area**:
  - 기본 4줄 높이, 최대 500자 입력 제한
  - 실시간 글자수 카운터 (`현재자/500자`)
  - 플레이스홀더 예시 제공: `"오늘 섭취한 음식을 입력해주세요 (예: 햄버거 1개, 치킨 2조각)"`
* **유효성 검사 (Validation)**:
  - 공백 입력 시: `"오늘 섭취한 음식을 입력해주세요"` 필드 에러 노출
  - 수량 오류(음수 등) 시: `"수량은 0보다 커야 합니다"` 경고
  - 에러 발생 시 입력 테두리 강조(`border-destructive`) 및 ARIA live 알림

### 4.2 자연어 파싱 및 칼로리 연산 엔진
* **구분자 지원**: 줄바꿈(`\n`), 쉼표(`,`), 플러스(`+`), 자연어 접속사 (`그리고`, `와`, `과`, `및`)
* **수량 파싱**:
  - 아라비아 숫자 및 소수점 (`2개`, `1.5조각`, `0.5공기`)
  - 한글 수량어 (`반`, `한`, `두`, `세`, `네`, `다섯` 등)
  - 생략 시 기본 1인분 간주
* **식품 데이터베이스 (100+종)**:
  - 패스트푸드, 치킨, 피자, 한식 찌개/식사, 면류, 중식, 일식, 양식, 분식, 베이커리, 디저트, 과일, 음료 및 주류
  - 유의어/줄임말 매칭 (`아아` -> `아이스아메리카노`, `생맥` -> `생맥주`, `빅맥` -> `햄버거` 등)

### 4.3 4단계 결과 화면 렌더링
1. **개별 음식 칼로리 뱃지**: 음식명 × 수량 및 항목별 계산 칼로리 카드
2. **총칼로리량 배너**: 불꽃 아이콘과 대형 강조 폰트로 총 kcal 시각화
3. **추천 운동 종목**: 걷기, 러닝, 자전거 종목 뱃지 노출
4. **운동 종목별 필요 시간 그리드**:
   - 걷기 (4.5 km/h, 4 kcal/분): 분/시간 환산
   - 러닝 (8 km/h, 10 kcal/분): 분/시간 환산
   - 자전거 (15 km/h, 7 kcal/분): 분/시간 환산

### 4.4 예외 처리 및 복구 UX
* **스켈레톤 UI**: 계산 대기 시 깜빡임(Pulse) 스켈레톤 카드 렌더링
* **오류 배너**: 미인식 음식 또는 네트워크/서버 오류 시 재시도(`RotateCcw`) 버튼 제공
* **테스트 키워드**: `error` 입력 시 에러 배너 재현 기능 탑재

### 4.5 최근 계산 기록 (History)
* LocalStorage 기반 최근 5건 계산 기록 유지
* 최근 기록 클릭 시 원클릭 재계산 및 전체 기록 삭제 기능 제공

### 4.6 Google Gemini AI 인공지능 식단 분석 및 맞춤 코칭 (NEW)
* **비정형 자연어 정밀 파싱**: 일기/대화형 복합 문장 및 외식 메뉴의 칼로리와 수량 정밀 산출
* **AI 영양 리포트**: 식단 총평 및 핵심 영양 요약 문장 제공
* **탄단지 밸런스 분석**: 탄수화물, 단백질, 지방 수준('낮음', '적정', '높음') 시각화
* **맞춤형 건강 팁**: 식단 구성에 따른 실천 가능한 조언 2가지 자동 제안
* **하이브리드 Fallback**: API 장애 시에도 로컬 DB 파서로 무중단 자동 전환

---

## 5. 비기능적 요구사항 (Non-Functional Requirements)
* **반응형 웹**: 모바일(375px~), 태블릿, 데스크톱 전 디바이스 최적화
* **웹 접근성 (a11y)**: `role="alert"`, `aria-live`, `aria-invalid`, `aria-describedby` 준수
* **테마 지원**: 다크 모드 및 라이트 모드 완벽 호환
* **AI 응답성**: Gemini 1.5 Flash 모델 활용으로 실측 1~2초대 빠른 추론 완료
